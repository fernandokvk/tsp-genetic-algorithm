public enum Parameters {;

    public enum Builder {
        NEAREST_NEIGHBOR,
        FARTHEST_INSERTION,
        CLOSEST_INSERTION
    }

    public enum Improver {
        OPT2_FIRST_IMPROVEMENT,
        OPT2_BEST_IMPROVEMENT,
        OPT3_FIRST_IMPROVEMENT,
        NONE
    }
}
